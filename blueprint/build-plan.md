# Build Plan

> Generated by `/adopt`. Shipped features are checked; the roadmap items from the
> interview are unchecked. Edit freely, then run `/overview`.

Keep it as a checklist. Run `/feature` with no number to spec the **next
unchecked** item, or `/feature 12` / `/feature "name"` to pick a specific one.
Completed features stay checked; append new items with the next unused number
rather than renumbering.

## Shipped

- [x] 1. WordPress Builder API rendering core - `fetchPageData` client, template
  components, SEO metadata from the API
- [x] 2. Core marketing pages - home, contact-us, difference pages, meet the
  team, our-facilities, newsroom, reviews, review-us, privacy-policy, thank-you,
  alumni-rehab-program, rehab-admission
- [x] 3. Location-served system - state/city/substance templates under
  `/location-served` driven by WordPress content
- [x] 4. Root catch-all route - parentless WordPress pages resolve at their own
  slug with static params, ISR, and real 404s (includes custom post types)
- [x] 5. Blogs and authors - blog listing/pagination and author detail pages from
  WordPress
- [x] 6. Facility detail pages - `/facility` templates
- [x] 7. Quiz - interactive assessment quiz
- [x] 8. PPC landing pages - `/ppc` variants for paid traffic
- [x] 9. ISR + revalidation webhook - 60s revalidate plus secret-protected
  `POST /api/revalidate`
- [x] 10. Third-party integrations - CallTrackingMetrics number swapping, Jotform
  embeds with resizing, TrustIndex reviews widget, gallery/marquee interactions
- [x] 11. Staging deploy pipeline - GitHub Actions build to standalone artifact,
  deploy to EC2 with pm2, release retention, and health check

## Roadmap

Most ongoing work is not roadmap features: it is small ad-hoc changes (content
updates, page tweaks, image swaps). Those run through `/fix`, not this list.
The items below are the occasional larger efforts.

> TODO (confirm): broad strokes; split into concrete items before spec'ing.

- [ ] 12. Additional WP-driven pages and templates as content needs arise (new
  location, condition, or PPC pages)
- [ ] 13. Performance and SEO pass - Core Web Vitals, caching, metadata, and
  structured data improvements
  - [ ] 13a. Sitemap and robots.txt - `src/app/sitemap.ts` built from the
    WordPress page inventory plus `src/app/robots.ts` with the sitemap pointer
  - [ ] 13b. Social share metadata - `metadataBase`, Open Graph and Twitter
    tags from Builder SEO data, default share image
  - [ ] 13c. Structured data - JSON-LD for facilities/locations
    (LocalBusiness), blog posts (Article), and breadcrumbs
  - [ ] 13d. Performance pass - measure Core Web Vitals, then targeted caching
    and loading fixes
- [ ] 14. Deploy and infra improvements - production pipeline hardening and
  automatic CI checks
