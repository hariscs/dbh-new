import React from 'react';
import Image from 'next/image';
import Link from 'next/link';
import Layout from '../layouts/Layout';

export default function LocationservedPillar() {
  return (
    <Layout>
      <div id="content" className="site-main post-1731 page type-page status-publish hentry">
        <div className="page-header">
          <h1 className="entry-title">location served (Pillar)</h1>
        </div>
        <div className="page-content"></div>
      </div>
    </Layout>
  );
}
